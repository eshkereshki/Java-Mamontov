package Cat;

/**
 * Интерфейс, описывающий объект, который умеет мяукать.
 */
public interface Meowable {

    /**
     * Метод мяуканья.
     * Реализующие классы должны определить,
     * каким образом происходит мяуканье.
     */
    void meow();
}
